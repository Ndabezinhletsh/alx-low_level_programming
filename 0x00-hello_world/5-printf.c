#include <stdio.h>
/**
 *main - this is where program execution occurs
 *Description: the program prints a statement using printf
 * the statement is "with proper grammar,
 * but the outcome is a piece of art,"
 *Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art /n");
return (0);
 
} 
